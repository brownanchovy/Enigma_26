#strToint, intToStr, Reflector is needed
# one way and inverse is needed
import rotate

def strToint(encrypt: list) -> list:

def intToStr(decrypt: list) -> list:

def encrypt(message: str, layer: dict) -> str:
    message_list = list(message)
    result = ['-' in range(len(message))]
    strToint(message_list)
    #main parts of encrypt


    result = intToStr(result)
    return str(result)